Summarize what this {{.Language}} {{.Kind}} named {{.Name}} represents.

```
{{.Body}}
```
{{- if .Doc}}

Documentation: {{.Doc}}
{{- end}}
{{- if .Methods}}

Methods:
{{- range .Methods}}
- {{if .Signature}}`{{.Signature}}`{{end}}{{if and .Signature .Doc}} — {{end}}{{.Doc}}
{{- end}}
{{- end}}

Respond with JSON in this exact format:
{
  "purpose": "One sentence: what this {{.Kind}} represents, from a caller's perspective",
  "behavior": "Lifecycle, mutability, and how instances behave across method calls",
  "invariants": ["invariants that hold across all method calls"],
  "security": ["security-relevant state, contracts, or data this type is responsible for"]
}

Field guidance:
- purpose: the concept the type models, not the syntactic shape. Write "a thread-safe counter", not "a struct with an int field".
- behavior: lifetime, mutability, thread-safety, cloneability, and any state transitions the methods enforce.
- invariants: conditions the type guarantees between method calls ("value is always non-negative"). Empty array if none apply.
- security: sensitive data held, trust boundaries crossed, validation responsibilities. Empty array if nothing security-relevant applies.
