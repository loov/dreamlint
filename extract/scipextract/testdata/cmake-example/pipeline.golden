fixture language: C++
units (7):
  [0] math.(Counter).Counter(49f6e7a06ebc5aa8) (callees=[])
  [1] math.add(9b79fb6aee4c0440) (callees=[])
  [2] math.(Counter).bump(b126dc7c1de90089) (callees=[math.add(9b79fb6aee4c0440)])
  [3] math.(Counter).value(455f465bc33b4cdf) (callees=[])
  [4] math.multiply(9b79fb6aee4c0440) (callees=[math.add(9b79fb6aee4c0440)])
  [5] math.is_even(83c267b20bac841a)+math.is_odd(83c267b20bac841a) (callees=[])
  [6] .main(b126dc7c1de90089) (callees=[math.(Counter).Counter(49f6e7a06ebc5aa8) math.(Counter).bump(b126dc7c1de90089) math.(Counter).value(455f465bc33b4cdf) math.multiply(9b79fb6aee4c0440) math.add(9b79fb6aee4c0440) math.is_even(83c267b20bac841a)+math.is_odd(83c267b20bac841a)])
types (1):
  math.Counter (type, methods=[math.(Counter).Counter(49f6e7a06ebc5aa8) math.(Counter).value(455f465bc33b4cdf) math.(Counter).bump(b126dc7c1de90089)])
external symbols (1):
  std::printf

== prompt 0 :: type=math.Counter :: pass=type_summary ==
You are analyzing a C++ type named Counter to summarize what it represents.

```
class Counter {
public:
    Counter();
    // bump increments the counter and returns the new value.
    int bump();
    // value returns the current counter.
    int value() const;

private:
    int n;
};

// is_even reports whether n is even via mutual recursion with is_odd.
bool is_even(int n);

// is_odd reports whether n is odd via mutual recursion with is_even.
bool is_odd(int n);

} // namespace math
```

Documentation: Counter accumulates bumps into a running total.

Methods:
- No documentation available.
- value returns the current counter.
- bump increments the counter and returns the new value.

Analyze this type and respond with JSON in this exact format:
{
  "purpose": "One sentence: what this type represents, from a caller's perspective",
  "behavior": "Lifecycle, mutability, and how instances behave across method calls",
  "invariants": ["invariants that hold across all method calls"],
  "security": ["security-relevant state, contracts, or data this type is responsible for"]
}

Field guidance:
- purpose: describe the concept the type models, not the syntactic shape (e.g. "a thread-safe counter", not "a struct with an int field")
- behavior: lifetime, mutability, thread-safety, cloneability, and any state transitions enforced by the methods
- invariants: conditions the type guarantees to hold between method calls (e.g. "value is always non-negative"); empty array if none apply
- security: sensitive data held, trust boundaries crossed, validation responsibilities; empty array if nothing security-relevant applies

== prompt 1 :: unit=math.(Counter).Counter(49f6e7a06ebc5aa8) :: pass=summary ==
You are analyzing a C++ function to create a summary of its behavior.

```
Counter::Counter() : n(0) {}
```

## Receiver Type: type Counter
Counter accumulates bumps into a running total.

```
class Counter {
public:
    Counter();
    // bump increments the counter and returns the new value.
    int bump();
    // value returns the current counter.
    int value() const;

private:
    int n;
};

// is_even reports whether n is even via mutual recursion with is_odd.
bool is_even(int n);

// is_odd reports whether n is odd via mutual recursion with is_even.
bool is_odd(int n);

} // namespace math
```

Purpose: test type purpose
Behavior: test type behavior
Type invariants:
- test type invariant

Other methods on Counter:
- value returns the current counter.
- bump increments the counter and returns the new value.

Analyze this function and respond with JSON in this exact format:
{
  "purpose": "One sentence describing what this function does",
  "behavior": "How it behaves - side effects, return conditions, error cases",
  "invariants": ["preconditions", "postconditions", "guarantees"],
  "security": ["security-relevant properties for callers to know"]
}

Field guidance:
- purpose: one sentence from the caller's perspective — what the function does, not how; do not restate the function name
- behavior: edge cases, side effects, non-obvious return value semantics, and error conditions; do not repeat the happy path already captured in purpose
- invariants: observable contracts — preconditions the caller must satisfy (e.g. "input must be non-empty"), postconditions guaranteed on success (e.g. "returned pointer is never nil"), and invariants that always hold; use an empty array if none apply
- security: include only if security-relevant: whether inputs are validated or sanitized, whether authentication or authorization is enforced, whether sensitive data is handled, whether output is escaped; use an empty array if nothing security-relevant applies

== prompt 2 :: unit=math.(Counter).Counter(49f6e7a06ebc5aa8) :: pass=baseline ==
You are reviewing C++ code for logical correctness.

```
Counter::Counter() : n(0) {}
```

## Receiver Type: type Counter
Counter accumulates bumps into a running total.

```
class Counter {
public:
    Counter();
    // bump increments the counter and returns the new value.
    int bump();
    // value returns the current counter.
    int value() const;

private:
    int n;
};

// is_even reports whether n is even via mutual recursion with is_odd.
bool is_even(int n);

// is_odd reports whether n is odd via mutual recursion with is_even.
bool is_odd(int n);

} // namespace math
```

Purpose: test type purpose
Behavior: test type behavior
Type invariants:
- test type invariant

Other methods on Counter:
- value returns the current counter.
- bump increments the counter and returns the new value.

## Function Summary
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

Does this function correctly implement what it claims to do? Look for:

Implementation vs. Intent:
- Behavior that contradicts the declared purpose, invariants, or documented error conditions
- A code path that returns success when it should return an error, or vice versa
- A precondition stated in the summary that the implementation does not actually enforce

Logic Errors:
- Inverted boolean conditions (== where != is needed, < where > is needed)
- Wrong logical operator (&& vs ||) that produces incorrect short-circuit behavior
- Off-by-one errors in loop bounds, index expressions, or size calculations
- Unreachable code following an unconditional return, panic, or continue
- A switch or if-else chain that silently omits a case that must be handled
- Integer overflow or underflow in arithmetic that is not guarded

Control Flow:
- A loop that cannot terminate under reachable inputs
- Early return that skips required cleanup or postcondition logic
- Error path that falls through to success-path code

Severity scale: "critical" = exploitable security flaw or data corruption; "high" = incorrect behavior in normal use; "medium" = incorrect in edge cases or degraded reliability; "low" = poor practice with high probability of causing future bugs; "info" = minor concern.

Respond with JSON. The "line" field must be the line number. The "code" field must contain the exact line of code where the issue occurs, copied verbatim from the code block above.

Example:
{"issues": [{"function": "ParseConfig", "line": 42, "code": "	query := \"SELECT * FROM users WHERE id=\" + id", "severity": "critical", "message": "SQL injection via string concatenation", "suggestion": "Use parameterized query"}]}

If no issues found, return {"issues": []}.

== prompt 3 :: unit=math.add(9b79fb6aee4c0440) :: pass=summary ==
You are analyzing a C++ function to create a summary of its behavior.

```
int add(int a, int b) {
    return a + b;
}
```

Analyze this function and respond with JSON in this exact format:
{
  "purpose": "One sentence describing what this function does",
  "behavior": "How it behaves - side effects, return conditions, error cases",
  "invariants": ["preconditions", "postconditions", "guarantees"],
  "security": ["security-relevant properties for callers to know"]
}

Field guidance:
- purpose: one sentence from the caller's perspective — what the function does, not how; do not restate the function name
- behavior: edge cases, side effects, non-obvious return value semantics, and error conditions; do not repeat the happy path already captured in purpose
- invariants: observable contracts — preconditions the caller must satisfy (e.g. "input must be non-empty"), postconditions guaranteed on success (e.g. "returned pointer is never nil"), and invariants that always hold; use an empty array if none apply
- security: include only if security-relevant: whether inputs are validated or sanitized, whether authentication or authorization is enforced, whether sensitive data is handled, whether output is escaped; use an empty array if nothing security-relevant applies

== prompt 4 :: unit=math.add(9b79fb6aee4c0440) :: pass=baseline ==
You are reviewing C++ code for logical correctness.

```
int add(int a, int b) {
    return a + b;
}
```

## Function Summary
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

Does this function correctly implement what it claims to do? Look for:

Implementation vs. Intent:
- Behavior that contradicts the declared purpose, invariants, or documented error conditions
- A code path that returns success when it should return an error, or vice versa
- A precondition stated in the summary that the implementation does not actually enforce

Logic Errors:
- Inverted boolean conditions (== where != is needed, < where > is needed)
- Wrong logical operator (&& vs ||) that produces incorrect short-circuit behavior
- Off-by-one errors in loop bounds, index expressions, or size calculations
- Unreachable code following an unconditional return, panic, or continue
- A switch or if-else chain that silently omits a case that must be handled
- Integer overflow or underflow in arithmetic that is not guarded

Control Flow:
- A loop that cannot terminate under reachable inputs
- Early return that skips required cleanup or postcondition logic
- Error path that falls through to success-path code

Severity scale: "critical" = exploitable security flaw or data corruption; "high" = incorrect behavior in normal use; "medium" = incorrect in edge cases or degraded reliability; "low" = poor practice with high probability of causing future bugs; "info" = minor concern.

Respond with JSON. The "line" field must be the line number. The "code" field must contain the exact line of code where the issue occurs, copied verbatim from the code block above.

Example:
{"issues": [{"function": "ParseConfig", "line": 42, "code": "	query := \"SELECT * FROM users WHERE id=\" + id", "severity": "critical", "message": "SQL injection via string concatenation", "suggestion": "Use parameterized query"}]}

If no issues found, return {"issues": []}.

== prompt 5 :: unit=math.(Counter).bump(b126dc7c1de90089) :: pass=summary ==
You are analyzing a C++ function to create a summary of its behavior.

```
int Counter::bump() {
    n = add(n, 1);
    return n;
}
```

## Receiver Type: type Counter
Counter accumulates bumps into a running total.

```
class Counter {
public:
    Counter();
    // bump increments the counter and returns the new value.
    int bump();
    // value returns the current counter.
    int value() const;

private:
    int n;
};

// is_even reports whether n is even via mutual recursion with is_odd.
bool is_even(int n);

// is_odd reports whether n is odd via mutual recursion with is_even.
bool is_odd(int n);

} // namespace math
```

Purpose: test type purpose
Behavior: test type behavior
Type invariants:
- test type invariant

Other methods on Counter:
- No documentation available.
- value returns the current counter.

## Called Functions

### math.add(9b79fb6aee4c0440)
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

Analyze this function and respond with JSON in this exact format:
{
  "purpose": "One sentence describing what this function does",
  "behavior": "How it behaves - side effects, return conditions, error cases",
  "invariants": ["preconditions", "postconditions", "guarantees"],
  "security": ["security-relevant properties for callers to know"]
}

Field guidance:
- purpose: one sentence from the caller's perspective — what the function does, not how; do not restate the function name
- behavior: edge cases, side effects, non-obvious return value semantics, and error conditions; do not repeat the happy path already captured in purpose
- invariants: observable contracts — preconditions the caller must satisfy (e.g. "input must be non-empty"), postconditions guaranteed on success (e.g. "returned pointer is never nil"), and invariants that always hold; use an empty array if none apply
- security: include only if security-relevant: whether inputs are validated or sanitized, whether authentication or authorization is enforced, whether sensitive data is handled, whether output is escaped; use an empty array if nothing security-relevant applies

== prompt 6 :: unit=math.(Counter).bump(b126dc7c1de90089) :: pass=baseline ==
You are reviewing C++ code for logical correctness.

```
int Counter::bump() {
    n = add(n, 1);
    return n;
}
```

## Receiver Type: type Counter
Counter accumulates bumps into a running total.

```
class Counter {
public:
    Counter();
    // bump increments the counter and returns the new value.
    int bump();
    // value returns the current counter.
    int value() const;

private:
    int n;
};

// is_even reports whether n is even via mutual recursion with is_odd.
bool is_even(int n);

// is_odd reports whether n is odd via mutual recursion with is_even.
bool is_odd(int n);

} // namespace math
```

Purpose: test type purpose
Behavior: test type behavior
Type invariants:
- test type invariant

Other methods on Counter:
- No documentation available.
- value returns the current counter.

## Function Summary
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

Does this function correctly implement what it claims to do? Look for:

Implementation vs. Intent:
- Behavior that contradicts the declared purpose, invariants, or documented error conditions
- A code path that returns success when it should return an error, or vice versa
- A precondition stated in the summary that the implementation does not actually enforce

Logic Errors:
- Inverted boolean conditions (== where != is needed, < where > is needed)
- Wrong logical operator (&& vs ||) that produces incorrect short-circuit behavior
- Off-by-one errors in loop bounds, index expressions, or size calculations
- Unreachable code following an unconditional return, panic, or continue
- A switch or if-else chain that silently omits a case that must be handled
- Integer overflow or underflow in arithmetic that is not guarded

Control Flow:
- A loop that cannot terminate under reachable inputs
- Early return that skips required cleanup or postcondition logic
- Error path that falls through to success-path code

Severity scale: "critical" = exploitable security flaw or data corruption; "high" = incorrect behavior in normal use; "medium" = incorrect in edge cases or degraded reliability; "low" = poor practice with high probability of causing future bugs; "info" = minor concern.

Respond with JSON. The "line" field must be the line number. The "code" field must contain the exact line of code where the issue occurs, copied verbatim from the code block above.

Example:
{"issues": [{"function": "ParseConfig", "line": 42, "code": "	query := \"SELECT * FROM users WHERE id=\" + id", "severity": "critical", "message": "SQL injection via string concatenation", "suggestion": "Use parameterized query"}]}

If no issues found, return {"issues": []}.

== prompt 7 :: unit=math.(Counter).value(455f465bc33b4cdf) :: pass=summary ==
You are analyzing a C++ function to create a summary of its behavior.

```
int Counter::value() const {
    return n;
}
```

## Receiver Type: type Counter
Counter accumulates bumps into a running total.

```
class Counter {
public:
    Counter();
    // bump increments the counter and returns the new value.
    int bump();
    // value returns the current counter.
    int value() const;

private:
    int n;
};

// is_even reports whether n is even via mutual recursion with is_odd.
bool is_even(int n);

// is_odd reports whether n is odd via mutual recursion with is_even.
bool is_odd(int n);

} // namespace math
```

Purpose: test type purpose
Behavior: test type behavior
Type invariants:
- test type invariant

Other methods on Counter:
- No documentation available.
- bump increments the counter and returns the new value.

Analyze this function and respond with JSON in this exact format:
{
  "purpose": "One sentence describing what this function does",
  "behavior": "How it behaves - side effects, return conditions, error cases",
  "invariants": ["preconditions", "postconditions", "guarantees"],
  "security": ["security-relevant properties for callers to know"]
}

Field guidance:
- purpose: one sentence from the caller's perspective — what the function does, not how; do not restate the function name
- behavior: edge cases, side effects, non-obvious return value semantics, and error conditions; do not repeat the happy path already captured in purpose
- invariants: observable contracts — preconditions the caller must satisfy (e.g. "input must be non-empty"), postconditions guaranteed on success (e.g. "returned pointer is never nil"), and invariants that always hold; use an empty array if none apply
- security: include only if security-relevant: whether inputs are validated or sanitized, whether authentication or authorization is enforced, whether sensitive data is handled, whether output is escaped; use an empty array if nothing security-relevant applies

== prompt 8 :: unit=math.(Counter).value(455f465bc33b4cdf) :: pass=baseline ==
You are reviewing C++ code for logical correctness.

```
int Counter::value() const {
    return n;
}
```

## Receiver Type: type Counter
Counter accumulates bumps into a running total.

```
class Counter {
public:
    Counter();
    // bump increments the counter and returns the new value.
    int bump();
    // value returns the current counter.
    int value() const;

private:
    int n;
};

// is_even reports whether n is even via mutual recursion with is_odd.
bool is_even(int n);

// is_odd reports whether n is odd via mutual recursion with is_even.
bool is_odd(int n);

} // namespace math
```

Purpose: test type purpose
Behavior: test type behavior
Type invariants:
- test type invariant

Other methods on Counter:
- No documentation available.
- bump increments the counter and returns the new value.

## Function Summary
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

Does this function correctly implement what it claims to do? Look for:

Implementation vs. Intent:
- Behavior that contradicts the declared purpose, invariants, or documented error conditions
- A code path that returns success when it should return an error, or vice versa
- A precondition stated in the summary that the implementation does not actually enforce

Logic Errors:
- Inverted boolean conditions (== where != is needed, < where > is needed)
- Wrong logical operator (&& vs ||) that produces incorrect short-circuit behavior
- Off-by-one errors in loop bounds, index expressions, or size calculations
- Unreachable code following an unconditional return, panic, or continue
- A switch or if-else chain that silently omits a case that must be handled
- Integer overflow or underflow in arithmetic that is not guarded

Control Flow:
- A loop that cannot terminate under reachable inputs
- Early return that skips required cleanup or postcondition logic
- Error path that falls through to success-path code

Severity scale: "critical" = exploitable security flaw or data corruption; "high" = incorrect behavior in normal use; "medium" = incorrect in edge cases or degraded reliability; "low" = poor practice with high probability of causing future bugs; "info" = minor concern.

Respond with JSON. The "line" field must be the line number. The "code" field must contain the exact line of code where the issue occurs, copied verbatim from the code block above.

Example:
{"issues": [{"function": "ParseConfig", "line": 42, "code": "	query := \"SELECT * FROM users WHERE id=\" + id", "severity": "critical", "message": "SQL injection via string concatenation", "suggestion": "Use parameterized query"}]}

If no issues found, return {"issues": []}.

== prompt 9 :: unit=math.multiply(9b79fb6aee4c0440) :: pass=summary ==
You are analyzing a C++ function to create a summary of its behavior.

```
int multiply(int a, int b) {
    int result = 0;
    for (int i = 0; i < b; ++i) {
        result = add(result, a);
    }
    return result;
}
```

## Called Functions

### math.add(9b79fb6aee4c0440)
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

Analyze this function and respond with JSON in this exact format:
{
  "purpose": "One sentence describing what this function does",
  "behavior": "How it behaves - side effects, return conditions, error cases",
  "invariants": ["preconditions", "postconditions", "guarantees"],
  "security": ["security-relevant properties for callers to know"]
}

Field guidance:
- purpose: one sentence from the caller's perspective — what the function does, not how; do not restate the function name
- behavior: edge cases, side effects, non-obvious return value semantics, and error conditions; do not repeat the happy path already captured in purpose
- invariants: observable contracts — preconditions the caller must satisfy (e.g. "input must be non-empty"), postconditions guaranteed on success (e.g. "returned pointer is never nil"), and invariants that always hold; use an empty array if none apply
- security: include only if security-relevant: whether inputs are validated or sanitized, whether authentication or authorization is enforced, whether sensitive data is handled, whether output is escaped; use an empty array if nothing security-relevant applies

== prompt 10 :: unit=math.multiply(9b79fb6aee4c0440) :: pass=baseline ==
You are reviewing C++ code for logical correctness.

```
int multiply(int a, int b) {
    int result = 0;
    for (int i = 0; i < b; ++i) {
        result = add(result, a);
    }
    return result;
}
```

## Function Summary
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

Does this function correctly implement what it claims to do? Look for:

Implementation vs. Intent:
- Behavior that contradicts the declared purpose, invariants, or documented error conditions
- A code path that returns success when it should return an error, or vice versa
- A precondition stated in the summary that the implementation does not actually enforce

Logic Errors:
- Inverted boolean conditions (== where != is needed, < where > is needed)
- Wrong logical operator (&& vs ||) that produces incorrect short-circuit behavior
- Off-by-one errors in loop bounds, index expressions, or size calculations
- Unreachable code following an unconditional return, panic, or continue
- A switch or if-else chain that silently omits a case that must be handled
- Integer overflow or underflow in arithmetic that is not guarded

Control Flow:
- A loop that cannot terminate under reachable inputs
- Early return that skips required cleanup or postcondition logic
- Error path that falls through to success-path code

Severity scale: "critical" = exploitable security flaw or data corruption; "high" = incorrect behavior in normal use; "medium" = incorrect in edge cases or degraded reliability; "low" = poor practice with high probability of causing future bugs; "info" = minor concern.

Respond with JSON. The "line" field must be the line number. The "code" field must contain the exact line of code where the issue occurs, copied verbatim from the code block above.

Example:
{"issues": [{"function": "ParseConfig", "line": 42, "code": "	query := \"SELECT * FROM users WHERE id=\" + id", "severity": "critical", "message": "SQL injection via string concatenation", "suggestion": "Use parameterized query"}]}

If no issues found, return {"issues": []}.

== prompt 11 :: unit=math.is_even(83c267b20bac841a)+math.is_odd(83c267b20bac841a) :: pass=summary ==
You are analyzing a C++ function to create a summary of its behavior.


## Functions (mutually recursive group)

```
bool is_even(int n) {
    if (n == 0) {
        return true;
    }
    return is_odd(n - 1);
}
```

```
bool is_odd(int n) {
    if (n == 0) {
        return false;
    }
    return is_even(n - 1);
}

} // namespace math
```


Analyze this function and respond with JSON in this exact format:
{
  "purpose": "One sentence describing what this function does",
  "behavior": "How it behaves - side effects, return conditions, error cases",
  "invariants": ["preconditions", "postconditions", "guarantees"],
  "security": ["security-relevant properties for callers to know"]
}

Field guidance:
- purpose: one sentence from the caller's perspective — what the function does, not how; do not restate the function name
- behavior: edge cases, side effects, non-obvious return value semantics, and error conditions; do not repeat the happy path already captured in purpose
- invariants: observable contracts — preconditions the caller must satisfy (e.g. "input must be non-empty"), postconditions guaranteed on success (e.g. "returned pointer is never nil"), and invariants that always hold; use an empty array if none apply
- security: include only if security-relevant: whether inputs are validated or sanitized, whether authentication or authorization is enforced, whether sensitive data is handled, whether output is escaped; use an empty array if nothing security-relevant applies

== prompt 12 :: unit=math.is_even(83c267b20bac841a)+math.is_odd(83c267b20bac841a) :: pass=baseline ==
You are reviewing C++ code for logical correctness.


## Functions (mutually recursive group)

```
bool is_even(int n) {
    if (n == 0) {
        return true;
    }
    return is_odd(n - 1);
}
```

```
bool is_odd(int n) {
    if (n == 0) {
        return false;
    }
    return is_even(n - 1);
}

} // namespace math
```


## Function Summary
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

Does this function correctly implement what it claims to do? Look for:

Implementation vs. Intent:
- Behavior that contradicts the declared purpose, invariants, or documented error conditions
- A code path that returns success when it should return an error, or vice versa
- A precondition stated in the summary that the implementation does not actually enforce

Logic Errors:
- Inverted boolean conditions (== where != is needed, < where > is needed)
- Wrong logical operator (&& vs ||) that produces incorrect short-circuit behavior
- Off-by-one errors in loop bounds, index expressions, or size calculations
- Unreachable code following an unconditional return, panic, or continue
- A switch or if-else chain that silently omits a case that must be handled
- Integer overflow or underflow in arithmetic that is not guarded

Control Flow:
- A loop that cannot terminate under reachable inputs
- Early return that skips required cleanup or postcondition logic
- Error path that falls through to success-path code

Severity scale: "critical" = exploitable security flaw or data corruption; "high" = incorrect behavior in normal use; "medium" = incorrect in edge cases or degraded reliability; "low" = poor practice with high probability of causing future bugs; "info" = minor concern.

Respond with JSON. The "line" field must be the line number. The "code" field must contain the exact line of code where the issue occurs, copied verbatim from the code block above.

Example:
{"issues": [{"function": "ParseConfig", "line": 42, "code": "	query := \"SELECT * FROM users WHERE id=\" + id", "severity": "critical", "message": "SQL injection via string concatenation", "suggestion": "Use parameterized query"}]}

If no issues found, return {"issues": []}.

== prompt 13 :: unit=.main(b126dc7c1de90089) :: pass=summary ==
You are analyzing a C++ function to create a summary of its behavior.

```
int main() {
    int sum = math::add(2, 3);
    int product = math::multiply(sum, 4);

    math::Counter counter;
    counter.bump();
    counter.bump();

    std::printf("%d %d even=%d\n", product, counter.value(), math::is_even(product));
    return 0;
}
```

## Called Functions

### math.(Counter).Counter(49f6e7a06ebc5aa8)
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

### math.(Counter).bump(b126dc7c1de90089)
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

### math.(Counter).value(455f465bc33b4cdf)
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

### math.multiply(9b79fb6aee4c0440)
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

### math.add(9b79fb6aee4c0440)
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

### math.is_even(83c267b20bac841a)+math.is_odd(83c267b20bac841a)
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

## External Functions Used

### std.printf
Signature: 
No documentation available.

Analyze this function and respond with JSON in this exact format:
{
  "purpose": "One sentence describing what this function does",
  "behavior": "How it behaves - side effects, return conditions, error cases",
  "invariants": ["preconditions", "postconditions", "guarantees"],
  "security": ["security-relevant properties for callers to know"]
}

Field guidance:
- purpose: one sentence from the caller's perspective — what the function does, not how; do not restate the function name
- behavior: edge cases, side effects, non-obvious return value semantics, and error conditions; do not repeat the happy path already captured in purpose
- invariants: observable contracts — preconditions the caller must satisfy (e.g. "input must be non-empty"), postconditions guaranteed on success (e.g. "returned pointer is never nil"), and invariants that always hold; use an empty array if none apply
- security: include only if security-relevant: whether inputs are validated or sanitized, whether authentication or authorization is enforced, whether sensitive data is handled, whether output is escaped; use an empty array if nothing security-relevant applies

== prompt 14 :: unit=.main(b126dc7c1de90089) :: pass=baseline ==
You are reviewing C++ code for logical correctness.

```
int main() {
    int sum = math::add(2, 3);
    int product = math::multiply(sum, 4);

    math::Counter counter;
    counter.bump();
    counter.bump();

    std::printf("%d %d even=%d\n", product, counter.value(), math::is_even(product));
    return 0;
}
```

## Function Summary
Purpose: test purpose
Behavior: test behavior
Invariants:
- test invariant

Does this function correctly implement what it claims to do? Look for:

Implementation vs. Intent:
- Behavior that contradicts the declared purpose, invariants, or documented error conditions
- A code path that returns success when it should return an error, or vice versa
- A precondition stated in the summary that the implementation does not actually enforce

Logic Errors:
- Inverted boolean conditions (== where != is needed, < where > is needed)
- Wrong logical operator (&& vs ||) that produces incorrect short-circuit behavior
- Off-by-one errors in loop bounds, index expressions, or size calculations
- Unreachable code following an unconditional return, panic, or continue
- A switch or if-else chain that silently omits a case that must be handled
- Integer overflow or underflow in arithmetic that is not guarded

Control Flow:
- A loop that cannot terminate under reachable inputs
- Early return that skips required cleanup or postcondition logic
- Error path that falls through to success-path code

Severity scale: "critical" = exploitable security flaw or data corruption; "high" = incorrect behavior in normal use; "medium" = incorrect in edge cases or degraded reliability; "low" = poor practice with high probability of causing future bugs; "info" = minor concern.

Respond with JSON. The "line" field must be the line number. The "code" field must contain the exact line of code where the issue occurs, copied verbatim from the code block above.

Example:
{"issues": [{"function": "ParseConfig", "line": 42, "code": "	query := \"SELECT * FROM users WHERE id=\" + id", "severity": "critical", "message": "SQL injection via string concatenation", "suggestion": "Use parameterized query"}]}

If no issues found, return {"issues": []}.

